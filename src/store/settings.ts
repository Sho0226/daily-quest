export type Settings = {
  animations: boolean;
  sound: boolean;
  highContrast: boolean;
};

export const DEFAULT_SETTINGS: Settings = {
  animations: true,
  sound: true,
  highContrast: false,
};
